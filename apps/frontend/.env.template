# Frontend local development env.
# Prefer sourcing the repository-root .env, then override Docker service URLs to localhost.

# App URLs
PORT=3000
RAILWAY_URL=http://localhost:8000
INGEST_URL=http://localhost:8000/ingest
CRAWLEE_API_URL=http://localhost:3345/crawl

# Redis
REDIS_URL=redis://default:your-strong-redis-password@localhost:6379

# Database
POSTGRES_ENDPOINT=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=postgres
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD="your-super-secret-and-long-postgres-password"
POSTGRES_SSL=false
POSTGRES_POOL_MAX=10

KEYCLOAK_DB_ENDPOINT=localhost
KEYCLOAK_DB_PORT=5433
KEYCLOAK_DB_DATABASE=postgres
KEYCLOAK_DB_USERNAME=postgres
KEYCLOAK_DB_PASSWORD="your-super-secret-and-long-postgres-password"
KEYCLOAK_DB_SSL=false

# MinIO / S3
LOCAL_MINIO=true
AWS_REGION=us-east-1
AWS_KEY=minioadmin
AWS_SECRET=minioadmin
S3_BUCKET_NAME=uiuc-chat
MINIO_ENDPOINT=http://localhost:10000
MINIO_PUBLIC_ENDPOINT=http://localhost:10000
NEXT_PUBLIC_S3_ENDPOINT=http://localhost:10000

# Option 2: SQLite (uncomment to use)
# SQLITE_DB_NAME=uiuc-chat-backend-sqlite.db

# Vector database — engine is resolved per project by the ConnectionManager:
# projects with an active qdrant_config use the Python backend /getTopContexts
# (Qdrant); everything else uses frontend Drizzle + pgvector (default).
QDRANT_COLLECTION_NAME=illinois-chat-qwen
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY="your-strong-key-here"

# Keycloak
KEYCLOAK_URL=http://localhost:8080/
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080/

# Keycloak Admin Client
KEYCLOAK_ADMIN_CLIENT_ID=admin-cli
KEYCLOAK_ADMIN_CLIENT_SECRET=
NEXT_PUBLIC_KEYCLOAK_REALM=illinois_chat_realm
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=illinois_chat
NEXT_PUBLIC_USE_ILLINOIS_CHAT_CONFIG=True
NEXT_PUBLIC_SIGNING_KEY=""

# Super admins (comma-separated emails). SUPER_ADMIN_EMAILS drives API-route
# authorization (server-only); the NEXT_PUBLIC_ variant is inlined into the
# client bundle for UI filtering — set both to the same value.
SUPER_ADMIN_EMAILS=
NEXT_PUBLIC_SUPER_ADMIN_EMAILS=

# Hosted/local model endpoints
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B
EMBEDDING_API_BASE=https://embedding.ilchat.mss.illinois.edu/v1
NCSA_HOSTED_API_KEY=
NCSA_HOSTED_VLM_BASE_URL=
# Router model for tool calling on the hosted vLLM (defaults to the NCSA default chat model).
# The vLLM deployment must run with: --enable-auto-tool-choice --tool-call-parser hermes
TOOL_ROUTER_MODEL_ID=
OLLAMA_SERVER_URL=
VLADS_OPENAI_KEY=
# Allow-list (comma-separated, lowercased) for per-project
# `embedding_config.provider` values. Mirrored by the backend.
ALLOWED_EMBEDDING_PROVIDERS=openai,ollama

# Encryption for project_external_connections configs (server-side only).
# Must match the backend's ENCRYPTION_MASTER_KEY.
# Generate with: python -c "import os, base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
ENCRYPTION_MASTER_KEY=

# Optional
NEXT_PUBLIC_ILLINOIS_CHAT_BANNER_CONTENT=
NEXT_PUBLIC_POSTHOG_HOST=
NEXT_PUBLIC_POSTHOG_KEY=
POSTHOG_API_KEY=not_a_key
